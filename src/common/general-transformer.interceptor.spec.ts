import { ApiResponse } from './general-transformer.interceptor';

describe('GeneralTransformerInterceptor', () => {
  it('should be defined', () => {
    expect(new ApiResponse()).toBeDefined();
  });
});
